/**
 * Open Media Disc (OMD) label generator.
 *
 * Renders printable album-art label sheets as SVG (sized in real inches) from an
 * OMD package. Cross-platform and dependency-free, so both the `omd label` CLI
 * and OMD Studio use the same logic.
 */

export * from './labelSheet.js';
export * from './packageLabel.js';
